{{#> layouts/base }} {{#*inline "content-block"}}

<div class="container">
    <div class="card col-12">
        <div class="card-header">
            <div class="card-title">Profil</div>
        </div>
        <div class="card-body">
            <b>Name:</b> {{req.user.name}}<br />
            <b>CharacterID:</b> {{req.user.charId}}<br />
            <b>Rang:</b> {{req.user.Role.name}} ({{req.user.Role.permissionLevel}})
        </div>
    </div>
</div>

{{/inline}} {{/layouts/base}}
